import classNames from 'classnames/bind';
import styles from './MovieItemHorizon.module.scss';

const cx = classNames.bind(styles);

function MovieItemHorizon() {
    return <div></div>;
}

export default MovieItemHorizon;
